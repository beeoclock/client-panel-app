import {DurationVersionTypeEnum} from "../enum/duration-version-type.enum";

export interface IDurationConfiguration {
	object: 'DurationConfiguration';
	durationVersionType: DurationVersionTypeEnum;
}
