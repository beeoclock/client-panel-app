export enum OrderStatusEnum {
  draft = 'draft',
  unconfirmed = 'unconfirmed',
  confirmed = 'confirmed',
  pending = 'pending',
  done = 'done',
  cancelled = 'cancelled',
  rejected = "rejected"
}
