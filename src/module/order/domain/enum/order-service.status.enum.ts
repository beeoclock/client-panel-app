export enum OrderServiceStatusEnum {
	requested = 'requested',
	accepted = 'accepted',
	inProgress = 'inProgress',
	done = 'done',
	rejected = 'rejected',
	deleted = 'deleted',
	cancelled = 'cancelled'
}
