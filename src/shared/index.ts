import {BooleanState} from '@shared/domain/boolean.state';
import * as _Interceptors from "./interceptors";
// import {Module} from "@shared/module";

export const Utility = {
  BooleanState,
  Interceptors: _Interceptors,
}

// export default Module;
