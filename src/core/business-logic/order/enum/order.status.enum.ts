export enum OrderStatusEnum {
	draft = 'draft',
	deleted = 'deleted',
	requested = 'requested',
	confirmed = 'confirmed',
	inProgress = 'inProgress',
	done = 'done',
	cancelled = 'cancelled',
	rejected = 'rejected',
	waitingForPayment = 'waitingForPayment',
}
