# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build

      - name: Install firebase tools
        run: sudo npm install -g firebase-tools

      - id: 'auth_00000000000000'
        name: '00000000000000: Auth to Google & Change Firebase Target & Deploy to firebase hosting'
        uses: google-github-actions/auth@v0.4.0
        with:
          credentials_json: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BEEOCLOCK_CLOUD_00000000000000 }}'
      - run: firebase use ${{ steps.auth_00000000000000.outputs.project_id }}
      - run: firebase target:apply hosting panel-beeoclock-cloud-00000000000000 ${{ steps.auth_00000000000000.outputs.project_id }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: >-
            ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BEEOCLOCK_CLOUD_00000000000000
            }}
          projectId: beeoclock-cloud-00000000000000
